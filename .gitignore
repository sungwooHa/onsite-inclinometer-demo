# NOTE: bridge/config.yaml 은 데모 편의를 위해 추적한다(QA 토큰 포함).
#   ⚠️ 원격 push 시 토큰이 노출되므로 로컬 커밋만 권장. 토큰 폐기/교체 시 갱신.

# Python
__pycache__/
*.pyc
.venv/
venv/

# Arduino 빌드 산출물
build/
*.bin
*.elf

# OS
.DS_Store
*.log

# 로컬 툴링(커밋 금지)
.claude/
.serena/
